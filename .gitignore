# Ignore object files
**/*.o

# Ignore executables without extensions (common on Linux/Unix)
**/*

# But don't ignore source files
!**/*.c
!**/*.h

# Ignore compiled binaries with known names (optional)
**/a.out
**/*.out
**/*.exe
